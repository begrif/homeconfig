
Files can be installed on a new host with:

        make install

Changes to notes files can be found and copied back here with:

        make find-updates 

Those changes are not checked in automatically to allow for any
needed anonymization processes. Dot files are excluded from
consideration when finding updates. 
